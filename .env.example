# remove.bg API key 池，逗号分隔配置多个 key，配额耗尽时自动切换
REMOVE_BG_API_KEYS=key1,key2,key3

# 访问密码（必填）：客户端 X-Access-Password header 必须等于此值
ACCESS_PASSWORD=your-secret-password

# 可选：auto / preview / full / 50MP
REMOVE_BG_SIZE=auto
